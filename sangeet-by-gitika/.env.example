# Supabase Configuration
# Get these values from: https://app.supabase.com/project/_/settings/api

# Supabase URL (Public - Safe to expose on frontend)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key (Public - Safe to expose on frontend)
# This key has limited permissions based on your RLS policies
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (Private - NEVER expose on frontend)
# ⚠️ CRITICAL: This key has FULL database access. Keep it secret!
# Only use in API routes and server-side code
# NEVER commit this to version control
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# JWT Secret for Admin Authentication
# Generate a secure random string: openssl rand -hex 32
# Keep this secret and consistent across deployments
JWT_SECRET=your_jwt_secret_key

# Production Deployment Notes:
# 1. Set these environment variables in your hosting platform (Vercel, Netlify, etc.)
# 2. NEVER commit .env.local or .env.production to git
# 3. The service_role key should ONLY be used server-side
# 4. Use different values for development and production
